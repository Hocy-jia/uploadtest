spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:mysql://localhost:3306/uploadtest
    username: root
    password: jh123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
  main:
    allow-bean-definition-overriding: true
  sql:
    init:
      mode: always
upload:
    directory: /Users/jiahao/Downloads/PictureMaterials/test
server:
  port: 8181

